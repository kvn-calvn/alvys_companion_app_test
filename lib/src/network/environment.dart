import '../utils/magic_strings.dart';

class AppEnvironment {
  //static final Environment environment = Environment.Dev;
  //static final Environment environment = Environment.QA;
  static const Environment environment = Environment.sandbox;
}
